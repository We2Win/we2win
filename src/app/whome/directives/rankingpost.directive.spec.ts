import { RankingpostDirective } from './rankingpost.directive';

describe('RankingpostDirective', () => {
  it('should create an instance', () => {
    const directive = new RankingpostDirective();
    expect(directive).toBeTruthy();
  });
});
